numpy>=1.24
scipy>=1.10
pytest>=7.0
yfinance>=0.2.40
pybind11>=2.11

# Web API
fastapi>=0.110
uvicorn[standard]>=0.27
httpx2>=2.0              # required by starlette's TestClient
# `pydantic[email]` pulls in the optional email-validator dependency
# required by Pydantic's `EmailStr` (used in src/schemas/user.py).
pydantic[email]>=2.5
pydantic-settings>=2.0.0
# Database (SaaS layer)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13
passlib[bcrypt]>=1.7
# Pin bcrypt < 4.1 to avoid the well-known passlib 1.7.4 compatibility warning.
bcrypt<4.1
